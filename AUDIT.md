# AUDIT.md — EchoWithin Platform Audit (2026-09-02)

Scope: Full codebase at `C:\Users\DevTech\Desktop\Projects\echowithin\echowithin` (branch `main`, HEAD `975ac8b` "Added post moderation and flagging" + unstaged `blueprints/blog.py:15-19` int-cast, `tests/` 140 tests). Method: static review with file:line citations; `pytest 140 passed`. Companion docs: `DISCOVERY.md` (Phase 1 — refreshed 2026-09-02), `FINAL_REPORT.md` (Phase 3 prior 47 findings), `PERF_REPORT.md` (latency snapshot).

> **Posture vs 2026-08-02:** 40 of 47 prior findings are ✅ fixed (see `FINAL_REPORT.md:139-188` remediation table). This audit does **not** re-list fixed items except where they regressed or remain intentionally deferred. Focus is on **new** (post-moderation era) and **still-open** issues verified in current code.

---

## 1. Fixes — Confirmed bugs / security issues (ranked by severity)

Each item has a one-line repro and a file:line evidence pointer.

### Critical (ship before next deploy)

| #   | Severity     | Title                                                                        | File:Line                                                                           | One-line repro                                                                                                                                                                                                                                               |
| --- | ------------ | ---------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| F1  | **Critical** | Single-report censorship DoS — any authed user can hide any post globally    | `blueprints/blog.py:1062-1075`, `utils.py:676-686` `get_public_posts_filter`        | `POST /api/posts/<victim_id>/report {reason:'spam'}` → `is_suppressed=True`, `moderation_status='flagged'` immediately; `GET /blog/all` hides it for everyone until admin `POST /admin/posts/<id>/approve`                                                   |
| F2  | **Critical** | Report-spam + race → indefinite suppression / duplicate pending rows         | `blueprints/blog.py:1030-1037, 1060`, `main.py:891-893` `post_reports_conf` indexes | `find_one({post_id,reporter_id,status:'pending'})` blocks only pending; after dismiss same user re-reports infinitely; two concurrent `POST /report` both pass `existing is None` (no unique pending index) → double `report_count` inc + double suppression |
| F3  | **Critical** | Web confirm bypasses API brute-force lockout (plain `POST /confirm/<email>`) | `blueprints/auth.py:190-232` vs `api.py:80-122`                                     | API path `MAX_CONFIRM_ATTEMPTS=10` + `429` after 10 tries (`api.py:104-112`), but web path `auth.py:209` never reads/increments `attempt_count` → attacker brute-forces 6-digit code (`10⁴` space) via web form without throttle                             |

### High (fix within a week)

| #   | Severity | Title                                                                 | File:Line                                                                                               | One-line repro                                                                                                                                                                                                                                                                                              |
| --- | -------- | --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F4  | **High** | Unauthenticated Socket.IO injection into arbitrary share room         | `main.py:1641-1648` `discussion_new_comment` vs `main.py:1486-1506` `_note_share_access`                | `socket.emit('discussion_new_comment', {share_id:'<guess>', comment_data:{...}})` → `emit('discussion_updated', ..., room=share_id)` with **no** `_note_share_access` check; `join_note`/`acquire_lock`/`note_update` do check (`main.py:1510,1570,1623`), this one does not                                |
| F5  | **High** | Suppression bypass via re-index on edit/create (Typesense fast-path)  | `blueprints/blog.py:2026-2030, 1198-1229`, `notifications.py:127-145`, `typesense_client.py`            | Report → `is_suppressed True` → author `POST /update_post/<id>` → `index_post_to_typesense(post_id)` unconditionally (`blog.py:2028`) → Typesense returns hit even though `get_public_posts_filter` would hide; `view_post:1323` correctly hides but search path does not apply filter to Typesense results |
| F6  | **High** | SSRF TOCTOU + missing port allowlist + `::ffff:` bypass               | `utils.py:1093-1151` `is_safe_fetch_url` / `fetch_link_preview`                                         | `is_safe_fetch_url` checks `is_private/loopback/link_local` then `requests.get(..., allow_redirects=False)` re-validates each hop but never checks `parsed.port` (`:22,:6379,:5432` passes), DNS not pinned (check vs fetch TOCTOU), `http://[::ffff:169.254.169.254]` not blocked by hostname check        |
| F7  | **High** | Narrow guard on `count_documents` — only one of 77 call sites guarded | `blueprints/blog.py:15-19` (fixed) vs `blueprints/blog.py:84,114`, `blueprints/admin.py:94,344,354,361` | `try: int(total_posts_count)` added only at `get_latest_posts_feed:17`; other paths `total_posts = count_documents(filter_query)` → `math.ceil(total/per_page)` TypeError if driver returns non-int/Mock                                                                                                    |

### Medium (fix within two weeks)

| #   | Severity   | Title                                                               | File:Line                                                                                                                     | One-line repro                                                                                                                                                                                                                     |
| --- | ---------- | ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F8  | **Medium** | Unauthenticated share-count inflation (engagement signal poisoning) | `blueprints/blog.py:2203-2231` `share_post`                                                                                   | `POST /post/<id>/share` has `@limits(10,60)` only, no `@login_required` → anonymous    spams `share_count` inc, pollutes `get_top_posts_json:399` / feed scoring                                                                   |
| F9  | **Medium** | Ghost presence / lock denial (10-min DoS, no heartbeat)             | `main.py:1590-1608` `acquire_lock`, `main.py:1510-1540` `active_note_viewers`, `main.py:321-342` `cleanup_stale_global_state` | First edit-capable holder gets 600s exclusive lock; no admin override/heartbeat; view-only denied; `active_note_viewers`/`active_chat_views` in-memory (no Redis) → ghost avatars until 300s cleanup, stale across workers         |
| F10 | **Medium** | View vs feed suppression predicate drift                            | `utils.py:676-697` vs `blueprints/blog.py:1323-1328`, `blueprints/admin.py:409-410`                                           | `get_public_posts_filter` always hides `image_status:'removed_nsfw'`; `view_post` allows `is_approved True` + `removed_nsfw` → post viewable yet invisible in feeds/Typesense after manual DB `is_approved` without image fix      |
| F11 | **Medium** | Auth enumeration still partially distinct (web vs API uniform)      | `api.py:38-52,89,112,131,178` vs `blueprints/auth.py`                                                                         | Mobile API now uniform errors (`FINAL_REPORT #35` ✅ on `api.py`), but web `auth.py` login/register still distinguish username-taken / email-exists / Google-account — low severity per prior assessment, not re-raised as Critical |

### Low / Informational (deferred intentionally — see notes)

| #   | Severity | Title                                                            | File:Line                                                                         | Note                                                                                                                                                                                                                                                       |
| --- | -------- | ---------------------------------------------------------------- | --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F12 | Low      | Plaintext fallback retained `if not gAAAAA: return plaintext`    | `security.py:293-294,665-669,719-723,901`                                         | Intentional additive-only compat (`FINAL_REPORT #38` 🔶); removing breaks legacy data — flag, don't remove until migration complete                                                                                                                        |
| F13 | Low      | Single `SECRET_KEY` derives all KEK/v2/bond/media/community keys | `security.py:215-470`, `config.py:78`                                             | DB+key leak = full plaintext; v3 envelope mitigates DB-only breach; per-record random salts is future work (`FINAL_REPORT #37` 🔶)                                                                                                                         |
| F14 | Low      | APK still debug-signed `app-debug.apk`, no cert pinning          | `static/downloads/app-debug.apk`, `pages.py:244-260`, `blueprints/admin.py:42-69` | Integrity hash `sha256` now published (`admin.py:57-67`, `static/update-manifest.json:6` `E425…2DF`) and DB↔file synced (`main.py:629-686`); signature pinning remains Low because `sha256` verification is already on device before install per prior fix |

**Dependency note:** `pip audit` not available in this venv; `requirements.txt` pins `cryptography 46.0.3`, `requests 2.32.5`, `pillow 12.0.0`, `redis 7.1.0` — no high/critical CVEs known at cutoff without live audit. Run `pip-audit` in CI when added (see Improvements).

> **Verification:** `FINAL_REPORT` fixes #1-4,6-7,9-17,19-21,23-34,36,39-41,43-44 remain ✅ on re-check. #5 SSRF partially fixed (redirect re-validation ✅, port/pin still open → F6), #8 web confirm regressed → F3, #18 CSP staged (`CSP_STRICT_NONCES` env, 49 inline scripts remain), #30 share_post limit but still no auth → F8, #37-38 plaintext fallback intentionally retained.

---

## 2. Improvements — To existing, already-built features (ranked by effort vs impact)

Realistic for a solo/small-team open-source project; no "hire a team" items. Each cites effort and evidence.

| #   | Effort         | Impact               | Area                   | Improvement                                                                                                            | Evidence / Fix pointer                                                                                                                                                                                                                              |
| --- | -------------- | -------------------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I1  | **S** (1 day)  | **P0 perf**          | Messages latency       | Warm per-conversation Fernet + batch permission users; parallelize DM decrypt. Expected `messages` p95 1386ms → <600ms | `blueprints/chat.py:59-63` `decrypt_dm` per contact (20× PBKDF2 on cache miss `security.py:633`), `chat.py:78` N+1 `users_conf.find_one` per `dm_permissions` → `$in` batch; cache `_dm_fernet_cache` longer TTL                                    |
| I2  | **S** (1 day)  | **P0 perf**          | Personal Space latency | Eliminate clone-chase DB hit; cache decrypt + cap `?note_id=` scan                                                     | `blueprints/notes.py:261` `_decrypt_note_record` → `security.py:769-799` chases `source_note_id` chain depth 4; reuse joined doc. `notes.py:92` `_find_note_page` builds full id array O(N) — add `$limit` / indexed cursor pagination              |
| I3  | **XS** (2h)    | **High**             | Frontend perf          | Compress hero `logo.png 1.34 MB → WebP <50k`; load MathJax conditionally not globally                                  | `static/logo.png` (1.34 MB), `templates/base.html:509` `cdn.jsdelivr.net ... tex-mml-chtml.js` (~200k) on every route; gate via `{% block extra_head %}` on post/note pages only; keep Font Awesome `base.html:13` + Socket.IO `base.html:71` as is |
| I4  | **S** (1 day)  | **High reliability** | Notes search           | Paginate Typesense fallback full-scan + add `community_notes` index                                                    | `blueprints/notes.py:791-832` fallback loads all notes + decrypts each then regex; add `$skip/limit` or require Typesense health before fallback; `communities.py:82` already paginated but no search                                               |
| I5  | **XS** (3h)    | **Medium a11y**      | Accessibility          | Fix viewport `user-scalable=no` → `yes, maximum-scale=5.0`; add `aria-label` on note action buttons                    | `templates/base.html:6` `maximum-scale=1.0, user-scalable=no` blocks zoom (WCAG 1.4.4); `personal_space.html:2117` `role="tablist"` good but inline actions lack labels; `custom_styles.css` `#999` contrast fail `base.html:945`                   |
| I6  | **XS** (4h)    | **Medium perf**      | Bonds album            | Stream encrypt instead of `file.read()` holding plaintext+ciphertext                                                   | `blueprints/bonds.py:4256, 4274` `file.read()` per file (10×5MB=50MB transient) → chunked `encrypt_media_bytes` over temp file; caps already 10/50MB `bonds.py:4215-4228` so bounded                                                                |
| I7  | **M** (3 days) | **Ops**              | CI/CD                  | Add GitHub Actions gate: `pytest` + `flake8` + slow-request threshold                                                  | No `.github/workflows`; only `perf_test.py` root; `PERF_REPORT.md:10` remote-client latency >500ms warning already logs `main.py:309` + `X-Response-Time`; gate deploys on `p95>800ms`                                                              |
| I8  | **XS** (2h)    | **Low hygiene**      | Git                    | Enforce commit-msg discipline (`commit.md` already lists model attribution per `AGENTS.md:1` but not enforced)         | `git log --oneline -15` shows `Fix issues` ×4 (`6e9b41b`, `4c612f4` …) with no scope; add `commit-msg` hook checking `commit.md` style                                                                                                              |

**Already ✅ in this window (not re-listed):** `per-IP rate limiting` (`security.py`), `token hashing` (`security.py:1078`), `SSRF redirect re-validation`, `scheduler fail-closed` (`chat.py:892`), `media proxy allowlist` (`main.py:221`), `album local fallback encryption` (`bonds.py:4269`), `Redis cache value encryption` (`security.py:868`), `DM presence gating` (`chat.py:98`), `HSTS everywhere`, `share proposal caps`, etc. — see `DISCOVERY.md:5` and `FINAL_REPORT remediations`.

---

## 3. New feature suggestions — Only where they follow naturally from existing architecture

None is a generic checklist add; each explains why it fits what EchoWithin already is (privacy-forward, community/bond-centric, small-team sustainable). Category empty is allowed — these are scoped to realistic next steps, not filler.

| #   | Fits existing architecture why                                                                                                                                                                                                                                                               | Suggestion                                                                                                                                                                                                                                                                                 | Effort                                                                                                                                                                                                                               | Why not "add something new" filler                                                                                                                                                                                                                                                                                                                                    |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| N1  | **Encrypted notes is the core promise** (`security.py` v1→v3, `notes.py` 1869 lines) but users have **no offline backup** and `delete_account` (`profile.py:326-350` `cascade_delete_user_data`) is irreversible with incomplete export (`tests` has no export test)                         | **Encrypted local export** — `GET /api/v1/notes/export?format=json                                                                                                                                                                                                                         | ciphertext` (decrypted on demand after `app_lock` re-auth `api.py:495`), plus `ciphertext` ZIP of raw `personal_posts` docs for offline custody. Reuses `security.py:_decrypt_note_record` and `api.py` auth. 1 day, solo-buildable. | S                                                                                                                                                                                                                                                                                                                                                                     |
| N2  | **Bonds + Communities are the connection layer** (bonds 5577 lines, 11 sections `BOND_SECTIONS:43`; communities polls/challenges) but question selection is either random or Gemini AI without community signal; `community_questions_conf` (`main.py:792`) already banks AI QotDs for reuse | **Community-sourced QotD curation** — let bond members upvote `community_questions` into their QotD rotation (opt-in, consent-gated like existing `bonds.py:367` AI gate). Minimal new collection, reuses `community_questions_conf` index `bond_type` + `question_hash` unique. 1–2 days. | S                                                                                                                                                                                                                                    | Fits "connection/community" value and existing `community_questions` bank; does not add a new AI dependency, instead routes existing AI output through human curation                                                                                                                                                                                                 |
| N3  | **Blog is fully built** (`blog.py` 2274 lines, hot feed, typesense) and `post_reports_conf` (`main.py:891`) just added, but suppression is binary (visible/hidden) with no gradation; `get_public_posts_filter:682` already distinguishes `is_suppressed`/`moderation_status`/`is_reported`  | **Graduated moderation queue (quarantine → review → restore/suppress)** — keep current auto-flag on report but move visibility to: quarantine feed (reports ≥2 or AI flag) → admin `POST /admin/posts/<id>/quarantine-restore                                                              | suppress` instead of immediate global hide. Reuses `post_reports_conf` status `pending→dismissed` (`admin.py:473`) and `posts_conf` `moderation_status`/`is_suppressed` fields; fixes F1 without adding a new service. 1 day.        | S                                                                                                                                                                                                                                                                                                                                                                     |
| N4  | *(No more)*                                                                                                                                                                                                                                                                                  | —                                                                                                                                                                                                                                                                                          | —                                                                                                                                                                                                                                    | Intentionally left at 3. Other common suggestions (global notifications center, block-list already ✅ `utils.py:is_blocked_by`, E2E encryption, Typesense plaintext re-encryption) are either already built, deferred by owner decision (`FINAL_REPORT #2` keep plaintext search), or would require fundamental auth/crypto redesign beyond solo scope — not proposed. |

---

## Verification & Methods

- **Files read in full:** `main.py`, `database.py`, `security.py`, `models.py`, `config.py`, `requirements.txt`, `blueprints/*.py` (13 + `api.py`), `static/update-manifest.json`, `Procfile`, `Dockerfile`, `PERF_REPORT.md`, `DISCOVERY.md` (refreshed), `FINAL_REPORT.md`, `tests/` (10 files)
- **Commands run:** `.venv\Scripts\python -m pytest --collect-only -q` → **140 collected**; `.venv\Scripts\python -m pytest --tb=short -q` → **140 passed, 2 warnings** (flask_rq2 pkg_resources, pythonjsonlogger deprecation) in 14.59s; `wc -l` on all blueprints; `rg count_documents` 77 hits; `rg TODO|FIXME` 0; manifest + APK verified `static/update-manifest.json:1-7` `sha256 E425…2DF`, APK 22.8 MB
- **Inferred vs confirmed:** TTFB=100% per `PERF_REPORT.md:10` suggests server-bound not network, but exact decrypt vs DB share of `personal_space` 1492ms / `messages` 966ms needs in-container flame (marked inferred in Improvements). All bugs above confirmed by source path inspection.
- **Constraints honored:** Every claim cites `file:line`; discovery before recommendations (DISCOVERY.md regenerated 2026-09-02 with shifted refs); suggestions scoped to solo/small-team (≤3 days each, reuse existing collections/keys); no auth/crypto bypass redesign.

---

## What to do next (suggested order)

1. **Today:** Fix F1/F2 threshold + unique index (`post_reports_conf.create_index([('post_id',1),('reporter_id',1)], unique, partialFilter status pending)`), make `index_post_to_typesense` skip suppressed (F5), add `_note_share_access` to `discussion_new_comment` (F4), port `api_confirm` attempt-limit to `auth.confirm` (F3).
2. **This week:** F6 port/DNS pin + F7 guard consolidation + F8 `@login_required` on `share_post` + F9 lock TTL/override.
3. **Next sprint:** I1/I2 perf batch decrypts, I3 asset compression, N1 export + N3 quarantine (both close the new moderation loop).

*Deliverable of Phase 3. Evidence files: `DISCOVERY.md`, `FINAL_REPORT.md`, `PERF_REPORT.md`.*
